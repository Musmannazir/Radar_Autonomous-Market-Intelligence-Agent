# VITE_API_BASE_URL: base URL of the real Radar backend (FastAPI wrapping the
# LangGraph pipeline). Defaults to http://localhost:8000 if unset — this only
# needs to be changed for a deployed backend.
VITE_API_BASE_URL="http://localhost:8000"
